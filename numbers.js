const score = 400
console.log(score)

const balance = new Number(50000)
console.log(balance)

console.log(balance.toString()) // changes number to string

console.log(balance.toFixed(2)) // converts number to decimal

const num = 32.3377
console.log(num.toPrecision(3))

const hundreds = 1000000000
console.log(hundreds.toLocaleString('en-IN'))