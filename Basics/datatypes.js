// datatypes are divided into two categories based on how data is stored and access in the memory 


// primitive data type (call by value)
// string, number, boolean, null, undefined, symbol, bigINT

// non primitive (reference type) data type
// arrays, objects, functions

// js is dynamically typed language
// because you don't need to declare the datatype
// no type safety

// let userEmail;
// the variable has undefined datatype

// const id = Symbol('123')
// const anotherId = Symbol('123')

// console.log(id === anotherId)
// console.table([id, anotherId])

// big int declaration
// const bigNumber = 2648478294897489748n

// arrays
const heroes = ["spiderman", "superman", "batman"]

// objects
let myObj = {
    name: "peter parker",
    age: 22,
    city: "new york"
}

// functions
const myFunction = function(){
    console.log("hello world")
}

// stack and heap memory

// STACK (primitive)
// HEAP (non - primitive)

